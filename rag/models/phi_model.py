# models/phi_model.py
# Aquí va la lógica para cargar y usar el modelo Phi

def load_phi_model():
    # TODO: Implementa la carga real del modelo Phi
    # Por ejemplo, usando transformers o la librería que corresponda
    model = None  # Reemplaza con la carga real
    return model

def generate_with_phi(model, prompt: str) -> str:
    # TODO: Implementa la generación con el modelo Phi
    # Por ejemplo, model.generate(prompt)
    return "Respuesta generada por Phi (placeholder)"
